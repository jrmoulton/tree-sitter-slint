
MemoryTile := Rectangle {
    border-radius: 8px;
    property <bool> open-curtain;

    Image {
        source: icon;
        width: parent.width - 16px;
        height: parent.height - 16px;
        x: 8px;
        y: 8px;
    }
}
